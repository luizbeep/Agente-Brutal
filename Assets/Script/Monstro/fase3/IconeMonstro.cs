using UnityEngine;
using Image = UnityEngine.UI.Image;

// script para alternar o icone que fica na barra de vida

public class IconeMonstro : MonoBehaviour
{
    public Image icone;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}